name: homework-1-test

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.20
 
      - name: build
        run: go build ./cmd/main.go

      - name: test
        run: go test -v ./...